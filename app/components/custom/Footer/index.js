import { FlatESLint } from "eslint/use-at-your-own-risk";

const Footer = (props) => {
  return (
    <div
      style={{
        display: "flex",
        position: "relative",
        bottom: 0,
        paddingTop: "3rem",
      }}
    >
      <div>Footer</div>
    </div>
  );
};

export default Footer;
